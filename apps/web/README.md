# Web/API

Next.js + React + TypeScript con pagina de estado local y GET /api/health. El endpoint solo valida configuracion: no conecta PostgreSQL. Desde la raiz: `node scripts/task.mjs dev:web`. El tablero de tornaguias esta pendiente. No ejecutar importaciones en peticiones web ni exponer credenciales al cliente.
